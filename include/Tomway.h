// tomway.h : Include file for standard system include files,
// or project specific include files.

#pragma once
#include <string>

namespace tomway
{
    void draw_main_menu(bool* main_menu);
    std::string get_save_location();
}